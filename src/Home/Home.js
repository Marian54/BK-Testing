import './Home.css';import * as React from 'react';import { Route } from 'react-router-dom';const Home = () => (	<nav className='home'>		<img id='parknow' src='./Images/Park.svg' alt='SMART PARKING' />		<p id='msgpark'>			What if you could use one parking app to handle it all? What if you could			add time without running back to the meter? Or reserve a space before you			leave home?		</p>		<Route			render={({ history }) => (				<button					id='parknowbtn'					type='button'					onClick={() => {						history.push('/Park');					}}				>					PARK NOW				</button>			)}		/>	</nav>);export default Home;